# Skew First-Principles — Tier-1 Markout (2026-06)

**Date:** 2026-06-15
**Data:** `skew_analytics_snapshot` backfill 2025-06-01 → 2026-06-11 (16,791 snapshots,
~100 watchlist tickers) on `option_wizard_local`. Harness: `reports/skew_markout.py`.
**Status:** In-sample validation folded into V1. Findings govern the read engine's
allowed directional language (see spec §7).

> **Update (2026-06-16, `feat/skew-hardening`).** Three changes invalidate the exact
> verdict tables below; they are regenerated by the re-run recorded at the end of this
> note:
> 1. **Regime left the verdict bucket key.** Buckets are now
>    `(asset_class, deviation_class, drive_class)` — regime as a coin-flip slicer
>    fragmented the ~13mo sample so few buckets cleared n≥20 (a cause of the empty prod
>    verdict table). Regime robustness stays in the per-quarter catastrophic gate.
> 2. **Confidence capped at `med`.** The borrow-clean subset is filtered on *current*
>    borrow applied to historical anchors (point-in-time borrow history is unavailable),
>    and borrow fee is the dominant confound (Muravyev-Pearson-Pollet 2025). No verdict
>    may claim `high`; the live lean basis states the limitation. So the `conf=high`
>    values in the pre-hardening table below can no longer occur.
> 3. **The markout is now scheduled** (18:45 ET weekdays) — previously it had no caller,
>    so the prod verdict table was empty and every lean was NEUTRAL regardless of edge.

## Method

Two hypotheses, per spec §7 step 2:

1. **Primary — RV mean-reversion.** Per `(asset_class, deviation_class)` bucket, mean
   forward ΔRR over T+20 trading days. Does extreme RR revert toward baseline?
2. **Secondary — directional, borrow-conditioned.** Per `(asset_class, deviation_class,
   drive_class)` bucket, mean **cross-sectionally-demeaned** T+20 forward stock
   return on the **borrow-clean** subset (`borrow_flag != hard_to_borrow`). A bucket
   earns `TRADABLE_BULL`/`TRADABLE_BEAR` only if |mean excess| ≥ 1%, n ≥ 20, and it
   survives the per-time-window (calendar-quarter) catastrophic-degradation gate.
   (Regime was dropped from this key on 2026-06-16 — see the update banner above.)

**Cross-sectional demeaning is load-bearing.** Raw forward returns are uniformly
positive in this up-trending backfill window (market beta) and the watchlist is
growth/high-beta heavy, so even SPY-subtraction leaves a beta>1 drift that paints
almost every bucket `TRADABLE_BULL`. Demeaning each name by the universe's same-date
mean forward return measures **separation vs peers** — the only thing that could be
skew edge rather than beta. This is consistent with Muravyev-Pearson-Pollet (2025 JFE):
after proper conditioning the residual single-name option edge is largely not
exploitable.

## Primary hypothesis — RR mean-reversion (forward ΔRR, T+20)

| Bucket | mean ΔRR | n |
|---|---:|---:|
| single_name RICH | **−0.0029** | 4,352 |
| single_name CHEAP | **+0.0514** | 1,472 |
| single_name NORMAL | +0.0010 | 7,356 |
| sector_etf RICH | −0.0049 | 164 |
| sector_etf CHEAP | +0.0329 | 62 |
| index_macro RICH | +0.0008 | 584 |
| index_macro CHEAP | +0.0012 | 98 |

**Read:** the sign is the textbook mean-reversion signature for single-names and
sector ETFs — **RICH skew drifts down, CHEAP skew drifts up** toward baseline. This
supports the tab's *relative-value* framing (the core, always-interpretive read). It is
descriptive, not a tradeable trigger on its own.

## Secondary hypothesis — directional verdicts (cross-sectional excess, borrow-clean)

**Re-run 2026-06-16** (regime out of key, confidence capped at `med`), backfill
2025-05-01 → 2026-06-16, 23,311 snapshots on `option_wizard_local`: of **36 populated
buckets — 25 NONE, 8 TRADABLE_BEAR, 3 TRADABLE_BULL.** Live leans across **98 watchlist
tickers: 66 NEUTRAL, 20 BULLISH_TILT, 12 BEARISH_TILT** — ~67% carry **no** lean, the
intended conservative posture. (Pre-hardening this table read 16 BEAR / 9 BULL across
regime-split buckets with `conf=high`; both are gone.)

Representative TRADABLE buckets (excess = demeaned T+20 forward return), top by |excess|:

| Bucket (asset / dev / drive) | verdict | excess | n | conf |
|---|---|---:|---:|---|
| sector_etf / NORMAL / CHASE | TRADABLE_BEAR | −0.0743 | 28 | med |
| single_name / NORMAL / CHASE | TRADABLE_BULL | +0.0660 | 1,674 | med |
| single_name / RICH / CHASE | TRADABLE_BULL | +0.0634 | 682 | med |
| index_macro / RICH / PANIC | TRADABLE_BEAR | −0.0627 | 270 | med |
| index_macro / NORMAL / PANIC | TRADABLE_BEAR | −0.0547 | 36 | med |
| index_macro / NORMAL / STRUCTURAL | TRADABLE_BEAR | −0.0386 | 626 | med |

No bucket carries `high` confidence — the cap (borrow-clean is a current-borrow
approximation) holds. Verdicts are now **non-empty in the live read path**: 32/98
tickers surface a tilt where pre-hardening every ticker was NEUTRAL because the markout
had no scheduler caller.

`confidence` reflects **sample size + separation magnitude only** — it is NOT an
out-of-sample claim. Every verdict here is in-sample over a single ~13-month window.

## Limitations (must stay loud in the UI)

- **In-sample.** No walk-forward / out-of-sample test. Xing-Zhang-Zhao and
  Cremers-Weinbaum both document decay; treat `TRADABLE_*` as a tilt, not a forecast.
- **Borrow history is shallow** (latest-per-run `uw_positioning`), so the borrow-clean
  subset is filtered on *current* borrow applied to historical anchors — an
  approximation, not a point-in-time guarantee. Because borrow fee is the dominant
  confound for option-signal predictability (Muravyev-Pearson-Pollet 2025), the
  "edge is not a borrow artifact" claim is soft, and verdict confidence is therefore
  **capped at `med`** (never `high`). Closing this needs a point-in-time borrow source.
- **Earnings is not point-in-time** in backfilled rows (`earnings_gate='unknown'`); it
  only gates the *live* lean, not the historical separation (which is earnings-agnostic).
- **`drive` is coarse** (PANIC/CHASE/STRUCTURAL). Regime is no longer a bucket
  dimension (dropped 2026-06-16); the canonical CRI level is now a context tag + a
  per-regime guard belongs to the catastrophic gate, not the key.

## Governance

The deterministic read's relative-value body stays interpretive in all cases. The
**directional lean** is NEUTRAL unless a `TRADABLE_*` verdict exists for the live
bucket AND the borrow + earnings gates pass — and even then it is surfaced with its
confidence + basis prominent and capped as a low/med-conviction tilt. The markout now
runs nightly (`skew_markout_refresh`, 18:45 ET) so verdicts refresh automatically;
re-run `skew_analytics_backfill` only when extending/recomputing the historical base.
