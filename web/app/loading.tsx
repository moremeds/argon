export default function Loading() {
  return (
    <main
      style={{
        padding: 24,
        color: "var(--text-muted)",
        fontFamily: "var(--font-mono)",
      }}
    >
      Loading dashboard…
    </main>
  );
}
