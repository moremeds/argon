export default function Loading() {
  return (
    <main
      style={{
        minHeight: "100vh",
        padding: 32,
        background: "var(--bg-base)",
        color: "var(--text-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 12,
        textTransform: "uppercase",
        letterSpacing: 1,
      }}
    >
      Loading US rates factor desk...
    </main>
  );
}
